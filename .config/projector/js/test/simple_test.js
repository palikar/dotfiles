const chai = require('chai');
const expect = require('chai').expect;
const sinon = require('sinon');
var rewire = require('rewire');


describe('Simple', function () {

    it('should pass all the time', function () {
        expect(1).to.be.equal(1);
    });

    
});
